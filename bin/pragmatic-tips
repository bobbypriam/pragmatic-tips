#!/usr/bin/env node

var generate = require('../lib/generate');

var tip = generate();

console.log('Pragmatic Programmer Tips');
console.log('#' + tip.number + ':', tip.title);
console.log(tip.description);
